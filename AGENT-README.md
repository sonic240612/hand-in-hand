# hand-in-hand 참여자 연결 프로그램

호스트 프로젝트를 내려받지 않고 **본인 PC의 Codex 계정으로 같은 세션을 이어서 실행**합니다. 이 폴더는 연결 프로그램만 포함합니다. 프로젝트 파일과 호스트의 인증 정보는 포함하지 않습니다.

1. Node.js 22 이상과 공식 Codex CLI를 준비합니다. Tailscale을 설치·로그인하고, 호스트의 기기 공유 초대가 있다면 먼저 수락합니다. 현재 검증 버전은 Codex CLI 0.153.4입니다.
2. 이 PC의 터미널에서 `codex login`으로 **본인 계정**에 로그인합니다.
3. 호스트가 보낸 초대 링크를 브라우저에서 열어 이름을 입력합니다.
4. `내 Codex 연결`을 눌러 연결 코드를 확인합니다.
5. 압축을 푼 폴더에서 다음 명령을 실행합니다. 주소는 실제 호스트 주소로 바꿉니다.

```powershell
npm run agent -- --host https://호스트.네트워크.ts.net:8443
```

6. 프로그램이 물으면 브라우저에 나온 연결 코드를 입력합니다. 별도 npm 패키지 설치는 필요 없습니다.
7. 브라우저의 **같은 세션**에서 다음 지시를 입력합니다. 연결 프로그램은 내 턴을 기다렸다가 본인의 인증으로 실행합니다.

연결 프로그램은 계속 실행해 두세요. 중지하려면 터미널에서 Ctrl+C를 누릅니다. 이후 같은 연결을 재개하려면 다음 명령을 사용합니다.

```powershell
npm run agent -- --host https://호스트.네트워크.ts.net:8443 --resume
```

Codex 로그인 정보는 공식 Codex의 로컬 저장소에 남습니다. `.hih-agent`에는 호스트 접속 권한과 이 공유 세션의 원본이 저장되므로 이 폴더는 공유하지 마세요. 호스트의 참여 권한 회수 후에는 기존 연결로 작업할 수 없습니다.

기본 원격 접속은 Tailscale입니다. 브라우저와 연결 프로그램 모두 Tailscale로 연결된 상태에서 같은 사설 주소를 사용합니다. **Tailscale 기기 접근과 hand-in-hand 세션 초대는 각각 필요합니다.** 같은 LAN만 사용할 때는 호스트가 알려 준 `http://호스트IP:4317` 주소를 사용합니다. 완성된 턴의 원본 기록이 있어야 다음 사람이 같은 세션으로 재개할 수 있으므로, 작업 중 강제 종료를 피하고 화면의 중단 기능으로 턴을 저장한 뒤 종료하세요.

서로 다른 계정으로 재개할 때 공급자가 세션 상태를 거절하면 실행을 멈춥니다. 계정 간 세션 재개와 구독 사용량 분리는 실제 두 계정으로 확인해야 합니다. 실패한 경우 요약이나 새 대화로 자동 대체하지 않습니다.
