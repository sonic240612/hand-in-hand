# hand-in-hand 참여자 연결 프로그램

호스트 프로젝트를 내려받지 않고 **본인 PC의 Codex 계정으로 같은 세션을 이어서 실행**합니다. 이 폴더는 연결 프로그램만 포함합니다. 프로젝트 파일과 호스트의 인증 정보는 포함하지 않습니다.

1. Node.js 22 이상과 공식 Codex CLI를 준비합니다. Tailscale을 설치·로그인하고, 호스트의 기기 공유 초대가 있다면 먼저 수락합니다. 현재 검증 버전은 Codex CLI 0.153.4와 0.154.0입니다.
2. 이 PC의 터미널에서 `codex login`으로 **본인 ChatGPT 계정**에 로그인합니다. 추가 과금 방지를 위해 API 키 인증은 차단합니다. OS 자격 증명 저장소만 사용하는 경우 `codex -c cli_auth_credentials_store="file" login`으로 로그인하세요.
3. 호스트가 보낸 초대 링크를 브라우저에서 열어 이름을 입력합니다.
4. `내 Codex 연결`을 눌러 연결 코드를 확인합니다.
5. 압축을 푼 폴더에서 다음 명령을 실행합니다. 주소는 실제 호스트 주소로 바꿉니다.

```powershell
npm run agent -- --host https://호스트.네트워크.ts.net:8443
```

6. 프로그램이 물으면 브라우저에 나온 연결 코드를 입력합니다. 별도 npm 패키지 설치는 필요 없습니다.
7. 브라우저의 **같은 세션**에서 다음 지시를 입력합니다. 연결 프로그램은 내 턴을 기다렸다가 본인의 인증으로 실행합니다.

연결 프로그램은 계속 실행해 두세요. 중지하려면 터미널에서 Ctrl+C를 누릅니다. 이후 같은 연결을 재개하려면 다음 명령을 사용합니다.

```powershell
npm run agent -- --host https://호스트.네트워크.ts.net:8443 --resume
```

Codex 로그인 정보는 공식 Codex의 로컬 저장소에 남습니다. `.hih-agent`에는 호스트 접속 권한과 이 공유 세션의 원본이 저장되므로 이 폴더는 공유하지 마세요. 호스트의 참여 권한 회수 후에는 기존 연결로 작업할 수 없습니다.

연결기는 `.hih-agent/codex-runtime/`에 별도의 세션·DB·쓰기 잠금을 보관하여 Codex 데스크톱 앱과의 충돌을 피합니다. 로그인 접근 토큰은 로컬 메모리로만 전달하며 호스트에 보내거나 실행기 폴더에 저장하지 않습니다. 기본 Codex의 모델·도구 설정도 복제하지 않습니다.

`active writer` 오류로 작업이 시작되지 않았다면 기록을 유지한 채 **다시 실행**할 수 있습니다. 구버전 연결기를 사용하고 있다면 이 연결 프로그램을 업데이트하고 다시 연결하세요. 실행 이후 저장이 불확실한 오류는 자동 재실행하지 않습니다.

Codex가 컨텍스트를 압축해도 같은 세션을 계속 사용합니다. 이전 JSONL 원문은 그대로 보관하고 Codex의 압축 상태를 뒤에 추가해 다음 참여자에게 전달합니다. AI가 실제로 사용하는 컨텍스트는 Codex가 압축한 내용이며, 앱이 별도 인계 요약이나 새 대화를 만드는 방식은 아닙니다.

구버전에서 **“세션 압축이 감지되어 동일 기록 검증을 중지했습니다.”** 오류로 막혔다면, 오류가 발생한 기기에서 연결기를 Ctrl+C로 중지하고 업데이트합니다. Git으로 설치했다면 같은 폴더에서 `git pull --ff-only` 후 위의 `--resume` 명령으로 재시작합니다. `.hih-agent` 폴더를 유지하고, `--data`를 사용했다면 기존 경로도 그대로 지정하세요. 호스트도 같은 수정 버전으로 실행되어야 합니다.

업데이트한 연결기는 로컬의 해당 턴 복구 파일을 호스트로 전달합니다. 호스트는 작성자, 기존 원문·세션 ID·버전, 실행 중단 기록을 검사하고 전체 상태를 백업한 뒤 복구합니다. AI 응답이나 도구 실행 등 불확실한 활동이 있으면 자동 복구하지 않습니다. 터미널에 **“중단된 세션 원본을 호스트에 복구했습니다.”**가 나오면 브라우저를 새로고침하고 해당 작업의 **다시 실행**을 누르세요. 복구 파일이 없거나 검증에 실패하면 차단을 유지합니다.

Codex가 사용자 메시지 형식으로 저장한 자체 중단 안내는 해당 턴의 메타데이터를 확인해 허용합니다. 원문·세션 ID 검사를 통과했지만 활동 검사에서 거절된 후보는 호스트의 `.hih/recovery-candidates/`에 별도 보관합니다. 후보 파일은 검사만을 위한 자료이며 활성 세션에 자동 반영하지 않습니다.

기본 원격 접속은 Tailscale입니다. 브라우저와 연결 프로그램 모두 Tailscale로 연결된 상태에서 같은 사설 주소를 사용합니다. **Tailscale 기기 접근과 hand-in-hand 세션 초대는 각각 필요합니다.** 같은 LAN만 사용할 때는 호스트가 알려 준 `http://호스트IP:4317` 주소를 사용합니다. 완성된 턴의 원본 기록이 있어야 다음 사람이 같은 세션으로 재개할 수 있으므로, 작업 중 강제 종료를 피하고 화면의 중단 기능으로 턴을 저장한 뒤 종료하세요.

서로 다른 계정으로 재개할 때 공급자가 세션 상태를 거절하면 실행을 멈춥니다. 계정 간 세션 재개와 구독 사용량 분리는 실제 두 계정으로 확인해야 합니다. 실패한 경우 요약이나 새 대화로 자동 대체하지 않습니다.
